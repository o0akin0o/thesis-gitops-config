# Dev environment values
replicaCount: 1

image:
  repository: ghcr.io/o0akin0o/gitops-thesis-demo
  tag: ec840913b8a639259024faa55e86a40a4f8bc5ff
  pullPolicy: Always

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: false

resources:
  requests:
    memory: "128Mi"
    cpu: "100m"
  limits:
    memory: "256Mi"
    cpu: "200m"

environment: development
